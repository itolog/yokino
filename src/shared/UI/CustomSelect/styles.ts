import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  customSelect: {
    background: '#233750',
    padding: '2%',
    border: 'none',
    borderRadius: '5px',
    color: 'whitesmoke',
    fontSize: '1rem',
    minWidth: '130px',
    outline: 'none',
  },

}));

export default useStyles;
