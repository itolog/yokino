import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    submitMail: {
      position: 'relative',
    },
    buttonProgress: {
      top: '50%',
      left: '50%',
      color: '#95eac7',
      position: 'absolute',
      marginTop: '-15px',
      marginLeft: '-20px',
    },
  }),
);